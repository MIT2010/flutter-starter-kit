name: core
description: "Shared infrastructure: error, env, logger, di, utils"
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.12.2 <4.0.0"
  flutter: ">=3.44.3"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.8
  fpdart: ^1.2.0
  get_it: ^9.2.1
  injectable: ^3.0.0
  talker_flutter: ^5.1.17
  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
