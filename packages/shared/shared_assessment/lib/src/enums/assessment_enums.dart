/// Tipe media yang bisa ada di soal
enum MediaType { video, audio, image }

/// Format konten untuk intro dan instruksi
enum ContentFormat { markdown, plainText }

/// Status sesi pengerjaan tes
enum SessionStatus { inProgress, completed, expired, abandoned }
