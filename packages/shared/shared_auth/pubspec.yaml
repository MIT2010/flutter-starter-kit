name: shared_auth
description: "Shared auth entities dan contracts lintas fitur"
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.12.2 <4.0.0"

dependencies:
  core:
    path: ../../core
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
