name: core_network
description: "HTTP client, interceptors, dan WebSocket manager"
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.12.2 <4.0.0"
  flutter: ">=3.44.3"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  dio: ^5.9.2
  connectivity_plus: ^7.1.1
  dart_pusher_channels: ^1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
