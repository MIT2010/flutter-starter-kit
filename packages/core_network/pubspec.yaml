name: core_network
description: "HTTP client, interceptors, WebSocket manager, security, dan offline queue"
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.12.2 <4.0.0"
  flutter: ">=3.44.3"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  core_storage:
    path: ../core_storage
  dio: ^5.9.2
  connectivity_plus: ^7.1.1
  dart_pusher_channels: ^1.3.1
  flutter_secure_app: ^1.0.7
  safe_device: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.0
  build_runner: ^2.15.1
