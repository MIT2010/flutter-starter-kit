name: feature_hooks
description: >-
  Hooks for the `feature` brick: pre_gen derives project_name from the
  target project's pubspec.yaml; post_gen wires the new screen into
  app/router.dart.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0
dependencies:
  mason: ^0.1.2
