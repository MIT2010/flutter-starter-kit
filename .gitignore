# Local Mason cache (per-machine; regenerated by `mason get`)
.mason/

# Compiled output of the `feature` brick's post_gen hook (regenerated on use)
bricks/feature/hooks/.dart_tool/
bricks/feature/hooks/build/

# Internal mission / agent brief — not part of the distributed kit
CLAUDE.md
